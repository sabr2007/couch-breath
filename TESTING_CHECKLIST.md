# ✅ Чек-лист тестирования Scheduler

Используйте этот чек-лист для проверки готовности тестов к запуску.

## 📦 1. Установка зависимостей

- [ ] Установлены тестовые зависимости
  ```bash
  pip install -r requirements-test.txt
  ```

- [ ] Проверена версия pytest
  ```bash
  pytest --version
  # Ожидается: pytest 8.0.0+
  ```

- [ ] Проверена установка pytest-asyncio
  ```bash
  python -c "import pytest_asyncio; print(pytest_asyncio.__version__)"
  ```

## 🗄️ 2. Настройка тестовой БД

- [ ] Создана тестовая база данных PostgreSQL
  ```bash
  createdb couch_breath_test
  ```

- [ ] Установлена переменная окружения TEST_DATABASE_URL
  ```bash
  export TEST_DATABASE_URL="postgresql://postgres:postgres@localhost:5432/couch_breath_test"
  echo $TEST_DATABASE_URL
  ```

- [ ] Применены миграции к тестовой БД
  ```bash
  python scripts/init_test_db.py
  ```

- [ ] Проверено подключение к БД
  ```bash
  psql $TEST_DATABASE_URL -c "\dt"
  # Должны быть видны таблицы: users, lessons, enrollments, user_progress, submissions, reminders, access_codes, support_questions
  ```

## 📁 3. Структура файлов

- [ ] Созданы все тестовые файлы:
  - [ ] `tests/__init__.py`
  - [ ] `tests/conftest.py`
  - [ ] `tests/test_queries.py`
  - [ ] `tests/test_scheduler.py`
  - [ ] `tests/test_scenarios.py`
  - [ ] `tests/test_edge_cases.py`
  - [ ] `tests/README.md`
  - [ ] `tests/EXAMPLES.md`

- [ ] Созданы конфигурационные файлы:
  - [ ] `pytest.ini`
  - [ ] `requirements-test.txt`
  - [ ] `.env.test.example`

- [ ] Созданы вспомогательные файлы:
  - [ ] `scripts/init_test_db.py`
  - [ ] `run_tests.sh` (опционально, для Linux/Mac)
  - [ ] `TESTING_QUICKSTART.md`
  - [ ] `TESTING_SUMMARY.md`
  - [ ] `TESTING_CHECKLIST.md` (этот файл)

## 🧪 4. Запуск тестов

### 4.1 Базовая проверка

- [ ] Тесты успешно обнаруживаются
  ```bash
  pytest --collect-only
  # Должно быть найдено 56+ тестов
  ```

- [ ] Запускаются все unit-тесты
  ```bash
  pytest tests/test_queries.py -v
  # Все тесты PASSED
  ```

- [ ] Запускаются все integration-тесты
  ```bash
  pytest tests/test_scheduler.py -v
  # Все тесты PASSED
  ```

- [ ] Запускаются все сценарные тесты
  ```bash
  pytest tests/test_scenarios.py -v
  # Все тесты PASSED
  ```

- [ ] Запускаются все edge-case тесты
  ```bash
  pytest tests/test_edge_cases.py -v
  # Все тесты PASSED
  ```

### 4.2 Полный запуск

- [ ] Запускаются ВСЕ тесты без ошибок
  ```bash
  pytest
  # Все тесты PASSED
  ```

- [ ] Тесты работают с маркерами
  ```bash
  pytest -m unit
  pytest -m integration
  pytest -m scenario
  pytest -m edge
  ```

## 📊 5. Покрытие кода

- [ ] Генерируется отчёт о покрытии
  ```bash
  pytest --cov=bot/services/scheduler.py --cov=bot/database/queries.py --cov-report=term-missing
  ```

- [ ] Покрытие scheduler.py ≥ 80%

- [ ] Покрытие queries.py ≥ 90%

- [ ] HTML-отчёт создаётся успешно
  ```bash
  pytest --cov=bot --cov-report=html
  # Открыть htmlcov/index.html
  ```

## 🔍 6. Проверка конкретных тестов

### 6.1 Unit-тесты SQL-запросов

- [ ] `test_get_users_ready_for_next_lesson_basic` — PASSED
- [ ] `test_get_users_ready_not_enough_time` — PASSED
- [ ] `test_get_users_ready_exactly_1_day` — PASSED
- [ ] `test_get_users_ready_already_unlocked` — PASSED
- [ ] `test_unlock_next_lesson_success` — PASSED
- [ ] `test_unlock_next_lesson_idempotent` — PASSED
- [ ] `test_get_users_for_reminder_soft_3_days` — PASSED
- [ ] `test_get_users_for_reminder_already_sent` — PASSED
- [ ] `test_log_reminder_creates_record` — PASSED
- [ ] `test_update_last_activity_clears_reminders` — PASSED

### 6.2 Integration-тесты

- [ ] `test_check_lesson_unlocks_sends_notification` — PASSED
- [ ] `test_check_lesson_unlocks_updates_enrollment` — PASSED
- [ ] `test_check_lesson_unlocks_idempotent` — PASSED
- [ ] `test_send_reminders_soft_3_days` — PASSED
- [ ] `test_send_reminders_strong_7_days` — PASSED
- [ ] `test_send_reminders_idempotent` — PASSED

### 6.3 Сценарные тесты

- [ ] `test_scenario_ideal_path` — PASSED
- [ ] `test_scenario_delay_3_days` — PASSED
- [ ] `test_scenario_full_course` — PASSED
- [ ] `test_scenario_fast_student` — PASSED

### 6.4 Edge Cases

- [ ] `test_edge_exactly_24_hours` — PASSED
- [ ] `test_edge_23_hours_59_minutes` — PASSED
- [ ] `test_edge_lesson_18_to_19_nonexistent` — PASSED
- [ ] `test_edge_concurrent_scheduler_runs` — PASSED
- [ ] `test_edge_bot_not_set` — PASSED

## 🚀 7. Производительность

- [ ] Все тесты выполняются < 30 секунд
  ```bash
  pytest --durations=10
  ```

- [ ] Нет медленных тестов (> 5 секунд)

- [ ] БД очищается корректно между тестами

## 📝 8. Документация

- [ ] README.md содержит полную документацию
- [ ] QUICKSTART.md содержит инструкции по быстрому старту
- [ ] SUMMARY.md содержит сводку по проекту
- [ ] EXAMPLES.md содержит примеры использования
- [ ] Все файлы имеют актуальные комментарии

## 🔧 9. Отладка

- [ ] Тесты запускаются с отладчиком
  ```bash
  pytest --pdb
  ```

- [ ] Тесты запускаются с подробным выводом
  ```bash
  pytest -vv -s
  ```

- [ ] Логи выводятся корректно
  ```bash
  pytest --log-cli-level=DEBUG
  ```

## 🎯 10. Специфичные сценарии

### 10.1 Тесты открытия уроков

- [ ] Урок открывается ровно через 1 день после завершения
- [ ] Урок НЕ открывается раньше 1 дня
- [ ] Урок открывается через 2+ дня
- [ ] Следующий урок НЕ открывается повторно
- [ ] 19-й урок НЕ создаётся после 18-го

### 10.2 Тесты напоминаний

- [ ] Мягкое напоминание отправляется через 3 дня неактивности
- [ ] Настойчивое напоминание отправляется через 7 дней
- [ ] Напоминания НЕ отправляются повторно
- [ ] Напоминания НЕ отправляются после 14 дней неактивности
- [ ] Напоминания очищаются при возвращении пользователя

### 10.3 Тесты идемпотентности

- [ ] Повторный вызов `check_lesson_unlocks()` не создаёт дубликатов
- [ ] Повторный вызов `send_reminders()` не отправляет дважды
- [ ] Повторный вызов `unlock_next_lesson()` не создаёт записи
- [ ] Параллельные вызовы scheduler не создают race conditions

### 10.4 Тесты обработки ошибок

- [ ] Ошибка отправки сообщения не останавливает обработку
- [ ] Отсутствующий бот (`_bot = None`) не вызывает ошибку
- [ ] Повреждённые данные в БД обрабатываются корректно
- [ ] Отсутствующие записи не вызывают ошибок

## 📦 11. CI/CD готовность

- [ ] Тесты можно запустить в изолированной среде
- [ ] Переменные окружения корректно обрабатываются
- [ ] Нет зависимостей от локальных файлов
- [ ] Тесты не требуют интерактивного ввода
- [ ] Все зависимости указаны в requirements-test.txt

## ✨ 12. Финальная проверка

- [ ] Все тесты проходят успешно
  ```bash
  pytest -v
  ```

- [ ] Покрытие кода достигает целевых значений
  ```bash
  pytest --cov=bot --cov-report=term
  ```

- [ ] Нет warnings или deprecation notices
  ```bash
  pytest -v --strict-warnings
  ```

- [ ] Код соответствует PEP 8 (опционально)
  ```bash
  flake8 tests/
  ```

- [ ] Документация актуальна и понятна

## 🎉 13. Готовность к использованию

- [ ] Все пункты выше выполнены
- [ ] Тесты готовы к запуску в production
- [ ] Команда знакома с документацией
- [ ] Настроен CI/CD pipeline (опционально)

---

## 📝 Отметки о выполнении

**Дата проверки:** _________________

**Проверяющий:** _________________

**Результат:**
- [ ] ✅ Все тесты готовы к использованию
- [ ] ⚠️ Есть замечания (указать ниже)
- [ ] ❌ Требуются доработки

**Замечания:**
________________________________
________________________________
________________________________

---

## 🆘 Если что-то не работает

1. **Проверьте переменные окружения:**
   ```bash
   echo $TEST_DATABASE_URL
   ```

2. **Проверьте подключение к БД:**
   ```bash
   psql $TEST_DATABASE_URL -c "SELECT 1"
   ```

3. **Пересоздайте БД:**
   ```bash
   dropdb couch_breath_test
   createdb couch_breath_test
   python scripts/init_test_db.py
   ```

4. **Проверьте зависимости:**
   ```bash
   pip list | grep pytest
   ```

5. **Запустите с отладкой:**
   ```bash
   pytest -vv --log-cli-level=DEBUG
   ```

6. **Смотрите документацию:**
   - `tests/README.md` — полная документация
   - `TESTING_QUICKSTART.md` — краткое руководство
   - `tests/EXAMPLES.md` — примеры использования

---

**Версия:** 1.0.0
**Последнее обновление:** 2026-01-07
